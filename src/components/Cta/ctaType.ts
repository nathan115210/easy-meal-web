export enum CtaType {
    Button = 'button',
    Submit = 'submit',
    Reset = 'reset',
}

export enum CtaVariants {
    Primary = 'primary',
    Secondary = 'secondary',
    Ghost = 'ghost',
}

export enum CtaSizes {
    Small = 'sm',
    Medium = 'md',
    Large = 'lg',
}